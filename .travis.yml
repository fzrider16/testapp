language: python
python:
  - "2.7"
env:
  - REQUIREMENTS=lowest
  - REQUIREMENTS=release
  - REQUIREMENTS=devel
  
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
